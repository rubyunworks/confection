#!/usr/bin/env ruby

ignore 'work', '.yardoc', 'doc', 'log', 'pkg', 'tmp', 'web'

desc "run specs"
task "spec" do
  cmd = "confectionless='true' qed"
  sh cmd
end

task "spec:cov" do
  cmd = "confectionless='true' qed -p coverage"
  sh cmd
end

