#!/usr/bin/env ruby

ignore 'work', '.yardoc', 'doc', 'log', 'pkg', 'tmp', 'web'

desc "run specs"
task "spec" do
  cmd = "config='legacy' qed"
  sh cmd
end

task "spec:cov" do
  cmd = "config='legacy' qed -p coverage"
  sh cmd
end

