#!/usr/bin/env ruby

ignore 'work', '.yardoc', 'doc', 'log', 'pkg', 'tmp', 'web'

desc "run specs"
task "spec" do
  cmd = "qed"
  sh cmd
end

task "spec:cov" do
  cmd = "config='none' qed -r./task/qed-coverage"
  sh cmd
end

